# Run storage

`vision-jev init-run` creates an immutable directory here. Large checkpoints and media should be stored externally; the run directory contains hashes and stable references. Completed runs are never edited except to add a signed reproduction link.
