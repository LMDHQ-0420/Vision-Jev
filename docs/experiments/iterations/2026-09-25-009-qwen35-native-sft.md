# Qwen3.5 原生 processor、主干与 SFT 门禁

- 日期：2026-09-25
- 代码状态：实现完成，结构验证通过
- 训练证据：32 样本过拟合与 12k pilot 均已测量
- 模型：`Qwen/Qwen3.5-0.8B`
- revision：`2fc06364715b967f1860aea9cf38778875588b17`

## 本次实现

- 增加显式模型快照下载，模型构造阶段强制只读本地文件。
- 接入 Qwen 原生 `AutoProcessor` 和 `AutoModelForMultimodalLM`。
- 图片上限约 256 个合并视觉 token；assistant 答案区之外全部 mask。
- 冻结视觉塔，只对白名单语言投影注入 rank-16 LoRA，共 4,030,464 个训练参数。
- 增加确定性 12k 构建器、单/双卡训练入口和结构化生成评测入口。
- 增加 Choice/Noul 答案格式测试；测试总数从 28 增至 30。

## 数据组合

12k manifest：`/mnt/sda1/sol_data/vision-jev/manifests/pilot-12k.jsonl`

- SHA-256：`61b8727d655fa1bae9806ffe60d05547f182239ac4f5d97e1e53673b6c37c943`
- train：11,347；eval：653，按 `group_id` 隔离
- Choice：9,600；Noul：1,800；Score：600
- 来源：完整 120k 配方按来源与任务比例确定性缩放

## 小批量过拟合

- 路径：`/mnt/sda1/sol_data/vision-jev/runs/qwen35-08b-overfit-32`
- 配置：`configs/train/sft_overfit.json`
- 硬件：RTX 4090 × 1，BF16
- 训练问题：32；更新步：120
- 首步 train loss：0.0529357
- 末步 train loss：0.0000088313
- 独立 32 问题 eval loss：0.2025784
- 用时：178.3 秒
- 结论：过拟合门禁通过，loss 可稳定下降并成功保存 LoRA checkpoint。

## 12k pilot

正式路径：`/mnt/sda1/sol_data/vision-jev/runs/qwen35-08b-sft-pilot-12k`

正式配置使用 RTX 4090 × 2、每卡 microbatch 1、gradient accumulation 16、global batch 32、2 epochs。

- 全局更新：710
- 用时：4,354.3 秒（72.6 分钟）
- 首步 train loss：0.1199220
- 末步 train loss：0.0215143
- eval loss：0.0443822
- adapter SHA-256：`29eb335c4b49077986e770033d639be2f848cbaa3365b1dc70ed427e950ad224`
- 训练结论：完整结束，无 OOM、NaN 或数据读取错误，LoRA checkpoint 正常保存。

第一次双卡尝试在 step 140 主动中止，证据保留在 `qwen35-08b-sft-pilot-12k-aborted-lr-schedule`。原因是 scheduler 总步数被重复按进程数折算，会使学习率在第一个 epoch 提前归零。正式 run 已恢复 Accelerate 所需的 pre-sharding 步数口径。

## 结构化生成评测

评测脚本：`scripts/evaluate_sft.py`

- 范围：全部 653 条 group-safe eval 样本
- 解码：greedy，最多 24 个新 token
- JSON 合法率：100.00%（653/653）
- 总体 exact match：75.80%（495/653）
- Choice：77.53%（414/534）
- Noul：80.00%（72/90）
- Score：31.03%（9/29）
- 平均生成延迟：0.335 秒/题；P50 0.289 秒；P95 0.440 秒
- 峰值 GPU 显存：1,978,577,920 bytes
- 逐样本结果 SHA-256：`7548bcf4f7e70992d04a372eaad1d1a7cbe77974b5773c5d9366851c3fe97d66`
- 汇总 SHA-256：`eb660c92ef4ec9015a196079ca66f0ec834a10af9d4ecafce601cfda84968ddf`

表现较强的来源包括 Multimodal-Mind2Web、TextVQA 和 WebLINX（本次 holdout 均为 100%），以及 ChartQA、VQAv2（94.59%）和 GQA（91.51%）。薄弱项是 KonIQ-10k/Score（31.03%）、RefCOCO+（35.00%）、RefCOCO/RefCOCOg（约 45%）和 NLVR（46.67%）。

结论：训练与输出格式门禁通过，Choice 和 Noul 已形成可用 pilot 基线；Score 与区域定位任务未达到扩大全量训练前的质量要求，需要先调整任务表示、配额或训练目标。

## 验证

- Ruff lint：通过
- mypy strict：通过（21 个源文件）
- pytest：30 passed
- 原生 processor 双样本实测：通过，输出包含 `input_ids`、`mm_token_type_ids`、`pixel_values` 和 `image_grid_thw`
- 模型加载与 LoRA 白名单审计：通过
- 完整结构化生成评测：653/653 完成，结果为 measured

## 下一步

- 为 Score 引入有序损失或更明确的等级语义，而不是仅使用字符串生成监督。
- 分析 RefCOCO 系列候选区域错误，确认 256 视觉 token 是否不足。
- 修正弱项后重复 12k 对照实验；未通过前不直接扩大到 120k。
