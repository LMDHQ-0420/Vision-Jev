# 2026-09-24-002：数据方案 v2 与下载管线

- 状态：measured / raw_download_complete / privacy_gate_open
- 关联：ADR-0003
- run_id：无训练 run

## 变化

采用新版 90k 公开 + 24k 程序/环境 + 6k 可核验增强配方，新增 AndroidControl/WebLINX，移除 SST 主训练配额。下载目录固定 `/mnt/sda1/sol_data/vision-jev`；API 配置只保存环境变量名，本地 `configs/local/api.toml` 被 Git 忽略。

下载器增加 HTTP 长度、目录固定字节数和 ZIP 结构校验；截断文件以 `.invalid-时间戳` 隔离，断点保留为 `.part`。Hugging Face 来源固定 commit revision。canonical v2 增加来源块、root、输入轨、图像预算、允许历史、候选生成 revision、质量与 teacher-only 门禁，并新增模型输入白名单。

已实现 Mind2Web、AndroidControl、WebLINX、RefCOCO oracle、GQA balanced、VQAv2、TextVQA、ChartQA、CLEVR、MNLI 的 canonical 适配入口。AndroidControl 直接解析官方 protobuf TFRecord，并按官方 split 隔离；WebLINX 改为 train 索引先行、文本 PII 预筛、确定性选择、按需下载 replay/截图，避免拉取 659,934 个文件。新增确定性公共 manifest 构建器，配额不足时只输出缺口报告，不伪造完成状态。

## 实际下载状态

10 个 active 来源的固定归档或训练索引均已有完成状态；SST 仅保留旧快照且不进入新配方。GQA 图像 ZIP 已续传到精确目录字节数 21,817,965,542，通过完整 CRC，SHA-256 为 `02ce5c49c793accd5305356de9c39a50f80a7aaac193b0203de30dbbc65bde62`，并已解压。WebLINX 的旧全量方式在 322/659,934 文件处主动停止，保留已下载字节不做破坏性删除；新管线从 7,082 条有有效截图的安全候选中固定选择 7,000 行，下载 6,600 张唯一截图，覆盖 827 个 train demo。

## 结果与限制

10 个来源均完成 canonical v2 与资产存在性校验，共 3,175,462 道：Mind2Web 7,350、AndroidControl 63,588、WebLINX 6,804、GQA 1,075,062、TextVQA 21,841、MNLI 412,051、RefCOCO 331,695、VQAv2 376,872、CLEVR 849,980、ChartQA 30,219。Mind2Web 覆盖 1,009 条轨迹；WebLINX 覆盖 801 个实际产出 demo。TextVQA 严格 8/10 一致性后，train Noul 实测只有 1,422 道，因此不降低质量门槛，而把公开块内 VQAv2/TextVQA 的题型子配额调整为 3.2k/4.8k 与 4.8k/1.2k；来源总量和公开块 76k Choice/14k Noul 均不变。

AndroidControl 首轮转换在最终原子校验中发现上游 accessibility forest 跨窗口重复 node ID；正式输出未被替换。适配器按逻辑 ID 去重后重跑通过，得到 train 63,006 / dev 582。GQA 得到 train 943,000 / dev 132,062。WebLINX 对上下文做 PII 拦截、对候选可见文本做脱敏；图像 PII 审计前统一写 `release_blocked=true`，不会被公共 manifest 构建器选中。

公共配额构建实跑后，除 WebLINX 外的 9 个来源均满足来源与题型双重配额，已可确定性选择 84,000 道（70k Choice / 14k Noul）；WebLINX 有 6,804 道 canonical train，但全部被 `release_blocked` 图像隐私门禁排除，因此报告中的唯一缺口是 `weblinx:choice=6000`。构建器按设计只写缺口报告、不发布不完整的 90k manifest。

这是数据工程证据，不是训练结果。完成下载不等于通过许可、PII、样本清洗和训练 manifest 验收；所有来源仍须生成质量/缺口报告。
